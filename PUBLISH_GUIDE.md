# NPM 发布指南

## 📦 准备发布 v1.3.0

### ✅ 已完成的准备工作

1. ✅ 版本号已更新: `1.2.13` → `1.3.0`
2. ✅ README 已更新（工具数量、版本历史）
3. ✅ TypeScript 已编译成功
4. ✅ 代码已提交到 Git
5. ✅ 所有优化工作已完成

### 📝 待执行步骤

#### 1. 推送到 GitHub

在 Windows PowerShell 或 CMD 中执行：

```bash
cd D:\Code\SensorTower_mcp_double_key\sensortower-mcp-npm
git push origin main
```

#### 2. 登录 npm（如果还没登录）

```bash
npm login
```

输入你的 npm 账号信息：
- Username: [你的 npm 用户名]
- Password: [你的密码]
- Email: [你的邮箱]

#### 3. 发布到 npm

```bash
npm publish
```

这会自动：
- 运行 `prepublishOnly` 脚本（重新编译）
- 打包 `dist/`, `bin/`, `README.md`
- 发布到 npm registry

#### 4. 验证发布

发布成功后，访问：
- npm 包页面: https://www.npmjs.com/package/sensor-tower-mcp-pro
- 检查版本是否显示为 `1.3.0`
- 检查 README 是否正确显示

#### 5. 测试安装

```bash
# 测试全局安装
npm install -g sensor-tower-mcp-pro@1.3.0

# 或测试 npx
npx sensor-tower-mcp-pro@1.3.0 --help
```

### 📊 发布内容

**包含的文件：**
- `dist/` - 编译后的 JavaScript 代码
- `bin/` - CLI 入口脚本
- `README.md` - 完整文档
- `package.json` - 包配置

**版本信息：**
- 版本: `1.3.0`
- 工具数: 28 个（从 43 个优化而来）
- 主要变更: 移除 11 个废弃工具，修复参数问题，添加文档警告

### ⚠️ 注意事项

1. **确保已登录 npm**: 使用 `npm whoami` 检查
2. **检查 .npmignore**: 确保不会发布不必要的文件
3. **版本号不可重复**: 如果发布失败，需要修改版本号
4. **发布后无法撤销**: 发布后 24 小时内可以 unpublish，之后只能废弃（deprecate）

### 🔍 故障排查

**如果发布失败：**

1. **权限问题**: 确保你是包的 owner 或 maintainer
   ```bash
   npm owner ls sensor-tower-mcp-pro
   ```

2. **版本冲突**: 版本号已存在
   ```bash
   # 修改 package.json 中的版本号
   npm version patch  # 或 minor, major
   ```

3. **网络问题**: 检查网络连接或使用代理
   ```bash
   npm config set registry https://registry.npmjs.org/
   ```

### 📢 发布后的推广

1. 在 GitHub 创建 Release
2. 更新项目文档
3. 在社交媒体分享更新

---

## 🎉 快速发布命令

如果一切准备就绪，只需执行：

```bash
cd D:\Code\SensorTower_mcp_double_key\sensortower-mcp-npm
git push origin main
npm publish
```

完成！🚀
